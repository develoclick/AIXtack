import data from "./data";

<GuideSection id="problema" title="Un calendario bonito que nadie puede cumplir" part="Entender el problema">

<QuickFacts data={data.quickFacts} />

<ProblemSection data={data.problem} />

Si es tu primera vez con un <Term id="asistente-ia">asistente de IA</Term>, no necesitas más que tu banco de ideas y una hoja de cálculo: lo demás se explica al usarlo.

</GuideSection>

<GuideSection id="resultado-esperado" title="Qué tendrás al terminar" part="Entender el problema">

<OutcomeSection data={data.outcome} />

</GuideSection>

<GuideSection id="para-quien" title="Para quién es esta guía, y para quién todavía no" part="Entender el problema">

<AudienceSection data={data.audience} />

</GuideSection>

<GuideSection id="caso-practico" title="El caso: un restaurante familiar con tres horas a la semana" part="Entender el problema">

Seguiremos a un solo negocio, inventado y así indicado en su tarjeta: sus tiempos y sus ideas sirven para practicar las cuentas.

<CaseStudy data={data.caseStudy} />

</GuideSection>

<GuideSection id="marco" title="Cuatro ideas para planificar con tu tiempo real" part="Preparar">

Dos de ellas, el <Term id="lote">lote</Term> y el <Term id="reciclaje">reciclaje</Term>, ahorran horas. Todas se apoyan en el <Term id="formato">formato</Term> de cada pieza, porque de él depende cuánto tarda.

<Callout variant="calculos" title="La hoja calcula, la IA reparte, tú decides">

Cada suma que decide si el calendario cabe sale de tu hoja de cálculo. Lo que la IA escriba es una propuesta que se contrasta.

</Callout>

<FrameworkSection data={data.framework} />

</GuideSection>

<GuideSection id="antes" title="Cómo se pide normalmente, y por qué no se cumple" part="Preparar">

<BeforeSection data={data.before} />

</GuideSection>

<GuideSection id="datos" title="Antes de abrir la IA: tus cuatro datos" part="Preparar">

Si todavía no tienes un banco de ideas, arma uno con [Ideas de contenido para tu negocio con IA](/marketing/guias/ideas-de-contenido-para-tu-negocio-con-ia).

<DataPreparation data={data.dataPreparation} />

<GuideImage slot={data.images.datos} />

</GuideSection>

<GuideSection id="metodo" title="El método: seis pasos, con la IA en cinco" part="Hacer">

Marca los pasos según avances; el avance se guarda solo en este navegador.

<StepSection data={data.method} checkable guide={data.metadata.slug} />

</GuideSection>

<GuideSection id="hoja" title="Paso 2: armar tu hoja de capacidad" part="Hacer">

Una <Term id="hoja-de-calculo">hoja de cálculo</Term> te dirá en un segundo si una semana cabe. Constrúyela con este prompt, que solo recibe la descripción de tus columnas, o copia la calculadora que viene después. Un <Term id="prompt">prompt</Term> es lo que le escribes a la IA para pedirle algo; los huecos entre llaves, como `{{COLUMNAS}}`, son <Term id="variable">variables</Term> que rellenas con tus datos.

<Callout variant="simple">

Piensa en un prompt como en la nota que le dejas a alguien que empieza hoy: si es clara, no tiene que adivinar.

</Callout>

<PromptCard prompt={data.prompts.formulas} proof={data.images.prueba1} evidence={data.evidence} />

<ComparisonTable table={data.comparisons.hoja} eyebrow="Ejemplo generado" />

Con «Copiar como tabla» pegas esta calculadora en la celda A1 de una hoja nueva: cambia los números por los tuyos y ya funciona.

<ComparisonTable table={data.comparisons.calculadora} eyebrow="Plantilla para copiar" />

<GuideImage slot={data.images.hoja} />

</GuideSection>

<GuideSection id="prompt" title="Paso 3: pedir el calendario" part="Hacer">

El prompt recibe tu banco, tu tiempo y tus días, y devuelve una tabla por semana con sus minutos.

<PromptCard prompt={data.prompts.calendario} proof={data.images.prueba2} evidence={data.evidence} />

</GuideSection>

<GuideSection id="primer-resultado" title="Lo que respondió la IA la primera vez" part="Hacer">

Antes de leer el análisis, prueba tú: suma los minutos de cada semana con tus tiempos por formato.

<ResultBlock data={data.firstResult} />

<GuideImage slot={data.images.primerResultado} />

</GuideSection>

<GuideSection id="analisis" title="Paso 4: recalcular y puntuar" part="Evaluar y mejorar">

Para comprobar la semana 3 en tu calculadora, escribe 3 imágenes con texto, 1 serie y 0 mensajes: el total sale 145 y la última fila dice «No». Una <Term id="rubrica">rúbrica</Term> es una tabla de criterios con puntos que se usa igual con todos los calendarios.

<ResultAnalysis data={data.analysis} rubric={data.rubric} />

<GuideImage slot={data.images.detecta} />

<Rubric data={data.rubric} />

Si prefieres una primera pasada de la IA, este prompt recalcula y puntúa con los mismos cinco criterios.

<PromptCard prompt={data.prompts.revision} proof={data.images.prueba3} evidence={data.evidence} />

</GuideSection>

<GuideSection id="iteracion" title="Paso 5: mover lo que no cabe" part="Evaluar y mejorar">

<IterationBlock data={data.iteration} />

<PromptCard prompt={data.prompts.ajuste} proof={data.images.prueba4} evidence={data.evidence} />

</GuideSection>

<GuideSection id="resultado-final" title="El calendario después del ajuste" part="Evaluar y mejorar">

<ResultBlock data={data.improvedResult} />

<ComparisonTable table={data.comparisons.final} eyebrow="Ejemplo generado" />

<GuideImage slot={data.images.final} />

</GuideSection>

<GuideSection id="adaptacion" title="Paso 6: reciclar lo ya publicado" part="Reutilizar y evitar errores">

Con el calendario en marcha, este prompt propone qué piezas volver a usar y cuándo.

<PromptCard prompt={data.prompts.reciclaje} proof={data.images.prueba5} evidence={data.evidence} />

<ComparisonTable table={data.comparisons.reciclaje} eyebrow="Ejemplo generado" />

</GuideSection>

<GuideSection id="errores" title="Errores que rompen un calendario" part="Reutilizar y evitar errores">

<CommonMistakes items={data.mistakes} />

</GuideSection>

<GuideSection id="verificacion" title="Antes de comprometerte: lo que verificas tú" part="Verificar y aplicar">

Un <Term id="dato-inventado">dato inventado</Term> puede ser una suma, una fecha o un feriado: la IA lo escribe con seguridad y solo tú lo comprobarás.

<HumanVerification data={data.verification} />

</GuideSection>

<GuideSection id="aplicacion" title="Después de publicar: sostener el calendario" part="Verificar y aplicar">

<ApplicationSteps data={data.application} />

</GuideSection>

<GuideSection id="limitaciones" title="Lo que este método no resuelve" part="Verificar y aplicar">

<Limitations data={data.limitations} />

Para redactar cada pieza del calendario, sigue con [Crear publicaciones para redes sociales con IA](/marketing/guias/crear-publicaciones-para-redes-sociales-con-ia).

</GuideSection>

<GuideSection id="conclusion" title="Lo que te llevas" part="Referencia">

<Conclusion data={data.conclusion} />

<EvidenceBlock data={data.evidence} />

</GuideSection>

<GuideSection id="glosario" title="Glosario de esta guía" part="Referencia">

<GlossarySection ids={data.glossary} />

</GuideSection>

<GuideSection id="faq" title="Preguntas que surgen al aplicarlo" part="Referencia">

<FAQ items={data.faq} />

</GuideSection>
