import data from "./data";

<GuideSection id="problema" title="Por qué tu afiche no se entiende de un vistazo" part="Entender el problema">

<QuickFacts data={data.quickFacts} />

<ProblemSection data={data.problem} />

No hace falta experiencia previa: un <Term id="asistente-ia">asistente de IA</Term> es solo un programa con el que conversas por escrito, y cada término se explica al usarlo.

</GuideSection>

<GuideSection id="resultado-esperado" title="Qué tendrás al terminar" part="Entender el problema">

<OutcomeSection data={data.outcome} />

</GuideSection>

<GuideSection id="para-quien" title="Para quién es esta guía, y para quién todavía no" part="Entender el problema">

<AudienceSection data={data.audience} />

</GuideSection>

<GuideSection id="caso-practico" title="El caso: una panadería con vitrina a la calle" part="Entender el problema">

El negocio es inventado, como indica su tarjeta: sus datos sirven para practicar el método.

<CaseStudy data={data.caseStudy} />

</GuideSection>

<GuideSection id="marco" title="Cuatro niveles y dos pruebas" part="Preparar">

Lo que se ve primero y lo que se ve después es la <Term id="jerarquia-visual">jerarquía visual</Term>. Cada nivel tiene un peso distinto, y el tercero contiene la <Term id="llamada-a-la-accion">llamada a la acción</Term>: lo único que debe hacer quien lo lee.

<FrameworkSection data={data.framework} />

<GuideImage slot={data.images.jerarquia} />

</GuideSection>

<GuideSection id="antes" title="Cómo se pide normalmente, y qué falla" part="Preparar">

<BeforeSection data={data.before} />

</GuideSection>

<GuideSection id="datos" title="Antes de abrir la IA: tus datos en la ficha" part="Preparar">

<DataPreparation data={data.dataPreparation} />

La ficha reparte esos datos en los cuatro niveles y es el recurso central de la guía. Con «Copiar como tabla» la pegas en una hoja de cálculo y la completas ahí.

<ComparisonTable table={data.comparisons.niveles} eyebrow="Plantilla para copiar" />

<GuideImage slot={data.images.ficha} />

</GuideSection>

<GuideSection id="metodo" title="El método: seis pasos, con la IA en cuatro" part="Hacer">

Los pasos se pueden marcar según los completes: tu avance se conserva solo en este navegador.

<StepSection data={data.method} checkable guide={data.metadata.slug} />

</GuideSection>

<GuideSection id="prompt" title="Paso 3: pedir el contenido por niveles" part="Hacer">

Un <Term id="prompt">prompt</Term> es el mensaje que le escribes a la IA. Los huecos entre llaves, como `{{ACCION}}`, son <Term id="variable">variables</Term>: escribes tus datos en el constructor y el texto se completa solo.

<Callout variant="simple">

Un prompt es como el encargo que le das a un empleado nuevo: si solo dices «hazme un afiche», tendrá que adivinar lo demás.

</Callout>

<PromptCard prompt={data.prompts.niveles} proof={data.images.prueba1} evidence={data.evidence} />

</GuideSection>

<GuideSection id="primer-resultado" title="Lo que respondió la IA la primera vez" part="Hacer">

Léelo con tu ficha al lado: fíjate en qué datos usó y en cuánto dice.

<ResultBlock data={data.firstResult} />

</GuideSection>

<GuideSection id="analisis" title="Puntuar el contenido con una rúbrica" part="Evaluar y mejorar">

Una <Term id="rubrica">rúbrica</Term> es una tabla de criterios con puntos que se usa igual con todos los afiches, para comparar con la misma vara.

<ResultAnalysis data={data.analysis} rubric={data.rubric} />

<Rubric data={data.rubric} />

</GuideSection>

<GuideSection id="iteracion" title="Paso 4: recortar con la prueba de los tres segundos" part="Evaluar y mejorar">

<IterationBlock data={data.iteration} />

<PromptCard prompt={data.prompts.recorte} proof={data.images.prueba2} evidence={data.evidence} />

</GuideSection>

<GuideSection id="resultado-final" title="El contenido después del recorte" part="Evaluar y mejorar">

<ResultBlock data={data.improvedResult} />

<GuideImage slot={data.images.antesDespues} />

</GuideSection>

<GuideSection id="adaptacion" title="Paso 5: el brief para quien maqueta" part="Diseñar">

Un <Term id="brief">brief</Term> es un resumen escrito de lo que hay que hacer, para quien lo va a hacer. Este traduce el contenido final a indicaciones de diseño; la parte del <Term id="contraste">contraste</Term> se comprueba con un verificador, no con la IA.

<PromptCard prompt={data.prompts.brief} proof={data.images.prueba3} evidence={data.evidence} />

<ComparisonTable table={data.comparisons.brief} eyebrow="Ejemplo generado" />

<GuideImage slot={data.images.brief} />

</GuideSection>

<GuideSection id="comparativa" title="Qué debe ser el protagonista según tu objetivo" part="Diseñar">

<ComparisonTable table={data.comparisons.protagonista} />

</GuideSection>

<GuideSection id="errores" title="Errores que hacen ilegible un afiche" part="Verificar y aplicar">

<CommonMistakes items={data.mistakes} />

</GuideSection>

<GuideSection id="verificacion" title="Paso 6: comprobar cada dato antes de imprimir" part="Verificar y aplicar">

El riesgo principal es el <Term id="dato-inventado">dato inventado</Term>: la IA lo escribe con seguridad y solo tú puedes comprobarlo contra tu realidad.

<HumanVerification data={data.verification} />

<PromptCard prompt={data.prompts.verificacion} proof={data.images.prueba4} evidence={data.evidence} />

<ComparisonTable table={data.comparisons.revision} eyebrow="Ejemplo generado" />

<GuideImage slot={data.images.distancia} />

</GuideSection>

<GuideSection id="aplicacion" title="Después de verificar: de la pantalla al papel" part="Verificar y aplicar">

El <Term id="sangrado">sangrado</Term> es solo uno de los datos que pide una imprenta, por eso se pregunta antes de crear el lienzo.

<ApplicationSteps data={data.application} />

</GuideSection>

<GuideSection id="limitaciones" title="Lo que este método no hace" part="Verificar y aplicar">

<Limitations data={data.limitations} />

Si todavía no decidiste la oferta, empieza por [Crear promociones con IA](/marketing/guias/crear-promociones-con-ia). Para comunicarla en redes o por mensaje, mira [Crear anuncios para tu negocio con IA](/marketing/guias/crear-anuncios-con-ia).

</GuideSection>

<GuideSection id="conclusion" title="Lo que te llevas" part="Referencia">

<Conclusion data={data.conclusion} />

<EvidenceBlock data={data.evidence} />

</GuideSection>

<GuideSection id="glosario" title="Glosario de esta guía" part="Referencia">

<GlossarySection ids={data.glossary} />

</GuideSection>

<GuideSection id="faq" title="Preguntas frecuentes" part="Referencia">

<FAQ items={data.faq} />

</GuideSection>

<GuideSection id="fuentes" title="Fuentes y verificación" part="Referencia">

<SourcesSection data={data.sources} />

</GuideSection>
