import data from "./data";

<GuideSection id="problema" title="Una cotización rápida que sale cara" part="Entender el problema">

<QuickFacts data={data.quickFacts} />

<ProblemSection data={data.problem} />

Si nunca usaste un <Term id="asistente-ia">asistente de IA</Term>, te basta con saber qué vendes y a qué precio: el resto se explica al usarlo.

</GuideSection>

<GuideSection id="resultado-esperado" title="Qué tendrás al terminar" part="Entender el problema">

<OutcomeSection data={data.outcome} />

</GuideSection>

<GuideSection id="para-quien" title="Para quién es esta guía, y para quién todavía no" part="Entender el problema">

<AudienceSection data={data.audience} />

</GuideSection>

<GuideSection id="caso-practico" title="El caso: una cocina a medida para la Sra. Paredes" part="Entender el problema">

Seguiremos a un solo negocio, inventado y así indicado en su tarjeta: sus precios, porcentajes y fechas sirven para practicar las cuentas.

<CaseStudy data={data.caseStudy} />

</GuideSection>

<GuideSection id="marco" title="Cuatro ideas para cotizar sin errores" part="Preparar">

<Callout variant="calculos" title="La hoja calcula, la IA redacta, tú decides">

Cada cifra sale de tu hoja de cálculo. La IA ordena las palabras con esas cifras y las condiciones son tuyas.

</Callout>

<FrameworkSection data={data.framework} />

</GuideSection>

<GuideSection id="antes" title="Cómo se suele pedir, y por qué no se comprueba" part="Preparar">

<BeforeSection data={data.before} />

Así puede verse el resultado de ese pedido. La columna de la hoja sale de la calculadora que armarás en el paso 2.

<ComparisonTable table={data.comparisons.ingenuo} eyebrow="Ejemplo ilustrativo" />

</GuideSection>

<GuideSection id="datos" title="Antes de abrir la IA: tus cuatro cosas" part="Preparar">

<DataPreparation data={data.dataPreparation} />

<GuideImage slot={data.images.datos} />

</GuideSection>

<GuideSection id="metodo" title="El método: siete pasos, con la IA en cinco" part="Preparar">

Al terminar cada paso, márcalo aquí: el avance se conserva solo en tu navegador.

<StepSection data={data.method} checkable guide={data.metadata.slug} />

</GuideSection>

<GuideSection id="hoja" title="Paso 2: armar tu hoja de cálculo" part="Calcular">

Una <Term id="hoja-de-calculo">hoja de cálculo</Term> suma sin equivocarse si sus fórmulas están bien escritas. Constrúyela con este <Term id="prompt">prompt</Term>, que solo recibe la descripción de tus columnas, o copia la calculadora que viene después. Los huecos entre llaves, como `{{PROGRAMA}}`, son <Term id="variable">variables</Term> que rellenas con lo tuyo.

<PromptCard prompt={data.prompts.formulas} proof={data.images.prueba1} evidence={data.evidence} />

<ComparisonTable table={data.comparisons.hoja} eyebrow="Ejemplo generado" />

Con «Copiar como tabla» pegas esta calculadora en la celda A1 de una hoja nueva: cambia las partidas y los porcentajes por los tuyos y ya funciona.

<ComparisonTable table={data.comparisons.calculadora} eyebrow="Plantilla para copiar" />

<GuideImage slot={data.images.hoja} />

</GuideSection>

<GuideSection id="entrevista" title="Paso 3: decidir tus condiciones" part="Redactar">

Las cifras se calculan; las condiciones se deciden. Son cuánto se paga y cuándo (el <Term id="anticipo">anticipo</Term> y el saldo), hasta cuándo vale el precio, qué incluye y qué no. Copia la tabla y complétala con las tuyas; lo que aún no decidiste queda «Pendiente».

<ComparisonTable table={data.comparisons.condiciones} eyebrow="Plantilla para copiar" />

Si te cuesta ponerlas por escrito, deja que la IA te entreviste.

<PromptCard prompt={data.prompts.condiciones} proof={data.images.prueba2} evidence={data.evidence} />

</GuideSection>

<GuideSection id="prompt" title="Paso 4: pedir el documento" part="Redactar">

Este prompt recibe la tabla de tu hoja y la de tus condiciones, y devuelve el documento con las cifras copiadas. En «TIPO» eliges cotización o propuesta: la segunda añade lo que entendiste del pedido y cómo trabajarás.

<PromptCard prompt={data.prompts.cotizacion} proof={data.images.prueba3} evidence={data.evidence} />

</GuideSection>

<GuideSection id="primer-resultado" title="Lo que respondió la IA la primera vez" part="Redactar">

Antes de leer el análisis, prueba tú: busca cada cifra del documento en tu hoja y cada frase en tus condiciones.

<ResultBlock data={data.firstResult} />

<GuideImage slot={data.images.primerResultado} />

</GuideSection>

<GuideSection id="analisis" title="Paso 5: contrastar con la hoja" part="Evaluar y mejorar">

Una <Term id="rubrica">rúbrica</Term> es una tabla de criterios con puntos que se usa igual con todos los documentos. Aquí se puntúa el del ejemplo.

<ResultAnalysis data={data.analysis} rubric={data.rubric} />

<GuideImage slot={data.images.revision} />

<Rubric data={data.rubric} />

Si prefieres una primera pasada de la IA, este prompt compara cada cifra con la hoja y puntúa con los mismos cinco criterios.

<PromptCard prompt={data.prompts.revision} proof={data.images.prueba4} evidence={data.evidence} />

</GuideSection>

<GuideSection id="iteracion" title="Paso 6: corregir solo lo señalado" part="Evaluar y mejorar">

<IterationBlock data={data.iteration} />

<PromptCard prompt={data.prompts.ajuste} proof={data.images.prueba5} evidence={data.evidence} />

</GuideSection>

<GuideSection id="resultado-final" title="El documento después del ajuste" part="Evaluar y mejorar">

<ResultBlock data={data.improvedResult} />

<GuideImage slot={data.images.final} />

</GuideSection>

<GuideSection id="errores" title="Errores que salen caros" part="Verificar y aplicar">

<CommonMistakes items={data.mistakes} />

</GuideSection>

<GuideSection id="verificacion" title="Paso 7: lo que verificas tú antes de enviar" part="Verificar y aplicar">

El nombre y la dirección de tu cliente son <Term id="dato-personal">datos personales</Term>: úsalos solo para esta cotización.

<HumanVerification data={data.verification} />

</GuideSection>

<GuideSection id="aplicacion" title="Después de enviar: aprender de cada cotización" part="Verificar y aplicar">

<ApplicationSteps data={data.application} />

</GuideSection>

<GuideSection id="limitaciones" title="Lo que este método no resuelve" part="Verificar y aplicar">

<Limitations data={data.limitations} />

Para contestar las preguntas que el cliente haga después, sigue con [Responder consultas de clientes con IA](/clientes/guias/responder-consultas-de-clientes-con-ia).

</GuideSection>

<GuideSection id="conclusion" title="Lo que te llevas" part="Referencia">

<Conclusion data={data.conclusion} />

<EvidenceBlock data={data.evidence} />

</GuideSection>

<GuideSection id="glosario" title="Glosario de esta guía" part="Referencia">

<GlossarySection ids={data.glossary} />

</GuideSection>

<GuideSection id="faq" title="Preguntas que surgen al aplicarlo" part="Referencia">

<FAQ items={data.faq} />

</GuideSection>
