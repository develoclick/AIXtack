import data from "./data";

<GuideSection id="problema" title="Un precio puesto por costumbre" part="Entender el problema">

<QuickFacts data={data.quickFacts} />

<ProblemSection data={data.problem} />

Si nunca usaste un <Term id="asistente-ia">asistente de IA</Term>, te basta con conocer tu producto y tus gastos: el resto se explica al usarlo.

</GuideSection>

<GuideSection id="resultado-esperado" title="Qué tendrás al terminar" part="Entender el problema">

<OutcomeSection data={data.outcome} />

</GuideSection>

<GuideSection id="para-quien" title="Para quién es esta guía, y para quién todavía no" part="Entender el problema">

<AudienceSection data={data.audience} />

Si ya tienes precios y costos y quieres diseñar una oferta, sigue con [Crear promociones y ofertas con IA](/marketing/guias/crear-promociones-con-ia).

</GuideSection>

<GuideSection id="caso-practico" title="El caso: una caja de seis galletas" part="Entender el problema">

Seguiremos a un solo negocio, inventado y así indicado en su tarjeta: sus costos y sus ventas sirven para practicar las cuentas.

<CaseStudy data={data.caseStudy} />

</GuideSection>

<GuideSection id="marco" title="Cuatro ideas para calcular un precio" part="Preparar">

<Callout variant="calculos" title="La hoja calcula, la IA interpreta, tú decides">

Cada cifra sale de tu hoja de cálculo. La IA explica lo que dicen los números y pregunta lo que falta; el precio lo eliges tú.

</Callout>

Dos palabras conviene fijarlas. Un <Term id="costo-fijo">costo fijo</Term> se paga se venda o no. El <Term id="recargo">recargo</Term> se calcula sobre el costo, y el margen, sobre el precio.

<FrameworkSection data={data.framework} />

</GuideSection>

<GuideSection id="antes" title="Cómo se suele pedir, y por qué se equivoca" part="Preparar">

<BeforeSection data={data.before} />

Este es un resultado posible de ese pedido. La columna de la derecha la calcula la hoja que armarás en el paso 2.

<ComparisonTable table={data.comparisons.ingenuo} eyebrow="Ejemplo ilustrativo" />

</GuideSection>

<GuideSection id="datos" title="Antes de abrir la IA: tus cuatro cosas" part="Preparar">

<DataPreparation data={data.dataPreparation} />

<GuideImage slot={data.images.datos} />

</GuideSection>

<GuideSection id="metodo" title="El método: siete pasos, con la IA en cinco" part="Preparar">

Tacha cada paso al cumplirlo: solo tu navegador recuerda el avance.

<StepSection data={data.method} checkable guide={data.metadata.slug} />

</GuideSection>

<GuideSection id="hoja" title="Paso 2: construir la calculadora de precio" part="Calcular">

Con las fórmulas bien escritas, una <Term id="hoja-de-calculo">hoja de cálculo</Term> no se equivoca al sumar. Puedes escribirlas con ayuda de este <Term id="prompt">prompt</Term>, que solo conoce la descripción de tus columnas, o copiar la calculadora de más abajo. Lo que va entre llaves, como `{{PROGRAMA}}`, es una <Term id="variable">variable</Term>: la rellenas con tus datos.

<PromptCard prompt={data.prompts.formulas} proof={data.images.prueba1} evidence={data.evidence} />

<ComparisonTable table={data.comparisons.hoja} eyebrow="Ejemplo generado" />

«Copiar como tabla» deja la calculadora lista para pegar en A1 de una hoja nueva: solo cambias la columna B.

<ComparisonTable table={data.comparisons.calculadora} eyebrow="Plantilla para copiar" />

<GuideImage slot={data.images.hoja} />

</GuideSection>

<GuideSection id="entrevista" title="Paso 3: ordenar tus costos" part="Calcular">

Si tus costos están dispersos, deja que la IA te entreviste. Te recordará lo que suele olvidarse, como tu tiempo o el empaque, sin ponerle valor. Cada dato que te devuelva va a la columna B de la hoja.

<PromptCard prompt={data.prompts.costos} proof={data.images.prueba2} evidence={data.evidence} />

</GuideSection>

<GuideSection id="prompt" title="Paso 4: pedir la lectura de la hoja" part="Interpretar">

Este prompt recibe la tabla de tu hoja y devuelve una explicación en cinco partes. No calcula ni recomienda un precio: eso queda para ti.

<PromptCard prompt={data.prompts.lectura} proof={data.images.prueba3} evidence={data.evidence} />

</GuideSection>

<GuideSection id="primer-resultado" title="Lo que respondió la IA la primera vez" part="Interpretar">

Antes de leer el análisis, prueba tú: busca cada cifra en tu hoja y pregúntate sobre qué base está calculado cada porcentaje.

<ResultBlock data={data.firstResult} />

<GuideImage slot={data.images.primerResultado} />

</GuideSection>

<GuideSection id="analisis" title="Paso 5: contrastar con la hoja" part="Interpretar">

Una <Term id="rubrica">rúbrica</Term> es una tabla de criterios con puntos que se usa igual con todas las lecturas. Aquí se puntúa la del ejemplo.

<ResultAnalysis data={data.analysis} rubric={data.rubric} />

<GuideImage slot={data.images.contraste} />

<Rubric data={data.rubric} />

</GuideSection>

<GuideSection id="iteracion" title="Y corregir solo lo señalado" part="Mejorar y probar">

<IterationBlock data={data.iteration} />

<PromptCard prompt={data.prompts.ajuste} proof={data.images.prueba4} evidence={data.evidence} />

</GuideSection>

<GuideSection id="resultado-final" title="La lectura después del ajuste" part="Mejorar y probar">

<ResultBlock data={data.improvedResult} />

</GuideSection>

<GuideSection id="adaptacion" title="Paso 6: probar escenarios" part="Mejorar y probar">

Tu margen depende de supuestos: cuánto vendes, cuánto cuestan los ingredientes. Antes de decidir, conviene ver cuánto se mueve si cambian. La IA dice qué celdas cambiar; la hoja da el resultado.

<PromptCard prompt={data.prompts.escenarios} proof={data.images.prueba5} evidence={data.evidence} />

<ComparisonTable table={data.comparisons.escenarios} eyebrow="Ejemplo generado" />

<ComparisonTable table={data.comparisons.resultados} eyebrow="Resultado en la hoja" />

<GuideImage slot={data.images.escenarios} />

</GuideSection>

<GuideSection id="errores" title="Errores que salen caros" part="Decidir y verificar">

<CommonMistakes items={data.mistakes} />

</GuideSection>

<GuideSection id="verificacion" title="Paso 7: lo que verificas tú antes de decidir" part="Decidir y verificar">

<HumanVerification data={data.verification} />

</GuideSection>

<GuideSection id="aplicacion" title="Después de decidir: mantener la hoja al día" part="Decidir y verificar">

<ApplicationSteps data={data.application} />

</GuideSection>

<GuideSection id="limitaciones" title="Lo que este método no resuelve" part="Decidir y verificar">

<Limitations data={data.limitations} />

Para enviar el precio a un cliente en un documento con condiciones, sigue con [Crear cotizaciones y propuestas con IA](/ventas/guias/crear-cotizaciones-y-propuestas-con-ia).

</GuideSection>

<GuideSection id="conclusion" title="Lo que te llevas" part="Referencia">

<Conclusion data={data.conclusion} />

<EvidenceBlock data={data.evidence} />

</GuideSection>

<GuideSection id="glosario" title="Glosario de esta guía" part="Referencia">

<GlossarySection ids={data.glossary} />

</GuideSection>

<GuideSection id="faq" title="Preguntas que surgen al aplicarlo" part="Referencia">

<FAQ items={data.faq} />

</GuideSection>
