import data from "./data";

<GuideSection id="problema" title="Todo es «para hoy» y la lista solo crece" part="Entender el problema">

<QuickFacts data={data.quickFacts} />

<ProblemSection data={data.problem} />

Si nunca usaste un <Term id="asistente-ia">asistente de IA</Term>, te bastan tus pendientes y una <Term id="hoja-de-calculo">hoja de cálculo</Term>: cada término se explica al aparecer.

</GuideSection>

<GuideSection id="resultado-esperado" title="Qué tendrás al terminar, y para quién es esta guía" part="Entender el problema">

<OutcomeSection data={data.outcome} />

<AudienceSection data={data.audience} />

</GuideSection>

<GuideSection id="caso-practico" title="El caso: una lavandería familiar con 25 pendientes" part="Entender el problema">

Seguiremos a un negocio inventado, como indica su tarjeta. Sus tareas, sus fechas y las horas de cada persona también lo son.

<CaseStudy data={data.caseStudy} />

</GuideSection>

<GuideSection id="marco" title="Cuatro ideas para ordenar sin adivinar" part="Preparar">

<FrameworkSection data={data.framework} />

Lo que vuelve cada semana o cada mes es una <Term id="tarea-recurrente">tarea recurrente</Term>: se anota una vez, con su frecuencia.

</GuideSection>

<GuideSection id="antes" title="Cómo se suele pedir, y por qué el plan no se sostiene" part="Preparar">

<BeforeSection data={data.before} />

<ComparisonTable table={data.comparisons.ingenuo} eyebrow="Ejemplo ilustrativo" />

</GuideSection>

<GuideSection id="datos" title="Paso 1: tus pendientes y tu tiempo" part="Preparar">

Escribe todo lo pendiente sin ordenar ni corregir. Abajo están la lista de Ana y el tiempo de cada persona.

<Callout variant="simple">

La <Term id="capacidad">capacidad</Term> no son las horas de trabajo de una persona, sino los minutos que le quedan para pendientes tras lo de siempre. Un plan que la ignora parece bueno hasta el martes.

</Callout>

<DataPreparation data={data.dataPreparation} />

<ComparisonTable table={data.comparisons.lista} eyebrow="Ejemplo ilustrativo" />

<GuideImage slot={data.images.lista} />

<ComparisonTable table={data.comparisons.disponibilidad} eyebrow="Plantilla para copiar" />

</GuideSection>

<GuideSection id="metodo" title="El método: siete pasos, con la IA en cuatro" part="Preparar">

Ve marcando los pasos que completes: tu avance queda únicamente en este navegador.

<StepSection data={data.method} checkable guide={data.metadata.slug} />

</GuideSection>

<GuideSection id="hoja" title="Paso 2: la hoja que hace las cuentas" part="Armar la hoja">

La hoja va antes que los prompts porque sus columnas son las que la IA rellenará. Tiene tres hojas: tareas, capacidad y plan.

<Callout variant="calculos" title="La prioridad y las sumas salen de tu hoja">

La prioridad sale de cruzar el impacto con la urgencia, no de una opinión, y los minutos de cada persona se suman con una fórmula. Lo que proponga la IA se contrasta antes de usarlo.

</Callout>

<ComparisonTable table={data.comparisons.matriz} eyebrow="Plantilla para copiar" />

<ComparisonTable table={data.comparisons.plantilla} eyebrow="Plantilla para copiar" />

Un <Term id="prompt">prompt</Term> es lo que le escribes a la IA para pedirle algo, y lo que va entre llaves, como `{{PROGRAMA}}`, es una <Term id="variable">variable</Term>: la rellenas con tus datos. Este prompt recibe solo la descripción de tus hojas, sin ninguna tarea.

<PromptCard prompt={data.prompts.formulas} proof={data.images.prueba1} evidence={data.evidence} />

<ComparisonTable table={data.comparisons.hoja} eyebrow="Ejemplo generado" />

<GuideImage slot={data.images.hoja} />

Antes de usar tus datos, prueba cada fórmula con los números de práctica: si no coincide con tu cuenta a mano, está mal.

</GuideSection>

<GuideSection id="prompt" title="Paso 3: pedir la tabla ordenada" part="Ordenar la lista">

Este prompt convierte tu lista en una tabla, pero no decide nada: no calcula la prioridad ni ordena las filas, y no puede inventar fechas ni minutos.

<PromptCard prompt={data.prompts.ordenar} proof={data.images.prueba2} evidence={data.evidence} />

</GuideSection>

<GuideSection id="primer-resultado" title="Lo que respondió la IA la primera vez" part="Ordenar la lista">

Léelo con tu lista al lado: cada fila tiene un id que puedes buscar en ella, y lo que falta aparece como [FALTA].

<ResultBlock data={data.firstResult} />

<GuideImage slot={data.images.primerResultado} />

</GuideSection>

<GuideSection id="analisis" title="Paso 4: contrastar la tabla con tu lista" part="Contrastar y corregir">

Una <Term id="rubrica">rúbrica</Term> es una tabla de criterios con puntos que se usa igual con cada tabla. Contrastar es buscar, para cada fila, la línea que escribiste y leer si dice lo mismo. Ten presente una <Term id="dependencia">dependencia</Term>: si una tarea necesita otra antes, dos filas están unidas aunque la tabla no lo diga.

<ResultAnalysis data={data.analysis} rubric={data.rubric} />

<GuideImage slot={data.images.contraste} />

<Rubric data={data.rubric} />

</GuideSection>

<GuideSection id="iteracion" title="Paso 4, segunda vuelta: corregir solo lo señalado" part="Contrastar y corregir">

<IterationBlock data={data.iteration} />

<PromptCard prompt={data.prompts.ajuste} proof={data.images.prueba3} evidence={data.evidence} />

</GuideSection>

<GuideSection id="resultado-final" title="Paso 5: la tabla corregida y su prioridad" part="Contrastar y corregir">

<ResultBlock data={data.improvedResult} />

Con la tabla corregida pegada en la hoja, las tres últimas columnas se calculan solas.

<ComparisonTable table={data.comparisons.prioridad} eyebrow="Calculado en la hoja" />

Los minutos de «Esta semana» y «Rutina» caben con holgura; los de «Programar con fecha» pesan más. Falta lo que la hoja no sabe: en qué día se hace cada cosa.

</GuideSection>

<GuideSection id="adaptacion" title="Paso 6: del orden al plan de la semana" part="Planificar la semana">

Con la prioridad calculada, la IA reparte. Este prompt recibe la tabla y la capacidad y devuelve un plan, pero no escribe ningún total: los sumas tú.

<PromptCard prompt={data.prompts.semana} proof={data.images.prueba4} evidence={data.evidence} />

Así puede verse el plan del caso.

<ComparisonTable table={data.comparisons.plan} eyebrow="Ejemplo generado" />

<GuideImage slot={data.images.plan} />

Falta el paso 7. En la hoja Capacidad, cada celda suma con una fórmula los minutos que el plan le da a una persona un día, y la compara con lo que tiene.

<ComparisonTable table={data.comparisons.control} eyebrow="Calculado en la hoja" />

Quedan fuera T15 y T22: duran 60 minutos y ninguna de sus personas tiene tanto en un día. La solución es partirlas en dos acciones con final claro. Y T06 y T19 esperan a que se sepa cuánto tardan.

</GuideSection>

<GuideSection id="errores" title="Errores que descuadran una semana" part="Verificar y aplicar">

<CommonMistakes items={data.mistakes} />

</GuideSection>

<GuideSection id="verificacion" title="Lo que verificas tú antes de dar el plan por bueno" part="Verificar y aplicar">

<HumanVerification data={data.verification} />

</GuideSection>

<GuideSection id="aplicacion" title="Después de la primera semana: mantener el sistema" part="Verificar y aplicar">

<ApplicationSteps data={data.application} />

</GuideSection>

<GuideSection id="limitaciones" title="Lo que este método no resuelve" part="Verificar y aplicar">

<Limitations data={data.limitations} />

Si tus pendientes incluyen publicar en redes, [Calendario de contenido con IA](/marketing/guias/calendario-de-contenido-con-ia) reparte esa parte con la misma lógica de tiempo. Y para las consultas que llegan por mensaje, está [Responder consultas de clientes con IA](/clientes/guias/responder-consultas-de-clientes-con-ia).

</GuideSection>

<GuideSection id="conclusion" title="Lo que te llevas" part="Referencia">

<Conclusion data={data.conclusion} />

<EvidenceBlock data={data.evidence} />

</GuideSection>

<GuideSection id="glosario" title="Glosario de esta guía" part="Referencia">

<GlossarySection ids={data.glossary} />

</GuideSection>

<GuideSection id="faq" title="Preguntas que surgen al aplicarlo" part="Referencia">

<FAQ items={data.faq} />

</GuideSection>
