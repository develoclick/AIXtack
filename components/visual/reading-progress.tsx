/** Barra de progreso de lectura (CSS, sin JavaScript). Aparece solo si el navegador admite `animation-timeline`. */
export function ReadingProgress() {
  return <div aria-hidden className="reading-progress" />;
}
